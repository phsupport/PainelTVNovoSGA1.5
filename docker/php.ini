; Minimal development php.ini
display_errors = On
display_startup_errors = On
error_reporting = E_ALL
log_errors = On
; Send PHP errors to Docker logs
error_log = /proc/self/fd/2

; Set default timezone (change if needed)
date.timezone = UTC
